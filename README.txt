=====================================
واجهة الوكيل - Agent Interface
نظام PHP لإدارة الأوامر والنتائج
=====================================

📋 المحتويات:
-------------
1. config.php       - إعدادات قاعدة البيانات
2. database.sql     - جداول قاعدة البيانات
3. api.php          - واجهة API
4. alwakeel.php     - الواجهة الرئيسية
5. README.txt       - هذا الملف

=====================================
⚙️ متطلبات التشغيل:
=====================================
✅ XAMPP (أو أي خادم PHP + MySQL)
✅ PHP 7.4 أو أحدث
✅ MySQL 5.7 أو أحدث
✅ متصفح ويب حديث

=====================================
📦 خطوات التثبيت:
=====================================

الخطوة 1: تثبيت XAMPP
----------------------
1. حمّل XAMPP من: https://www.apachefriends.org/
2. ثبّت XAMPP في: C:\xampp
3. شغّل XAMPP Control Panel

الخطوة 2: نسخ الملفات
----------------------
1. انسخ جميع ملفات المشروع إلى:
   C:\xampp\htdocs\alwakeel_interface\

2. يجب أن تكون الملفات كالتالي:
   C:\xampp\htdocs\alwakeel_interface\config.php
   C:\xampp\htdocs\alwakeel_interface\database.sql
   C:\xampp\htdocs\alwakeel_interface\api.php
   C:\xampp\htdocs\alwakeel_interface\alwakeel.php

الخطوة 3: إنشاء قاعدة البيانات
--------------------------------
1. شغّل Apache و MySQL من XAMPP Control Panel
2. افتح المتصفح واذهب إلى: http://localhost/phpmyadmin
3. اضغط على "New" لإنشاء قاعدة بيانات جديدة
4. اسم القاعدة: alwakeel_db
5. Collation: utf8mb4_unicode_ci
6. اضغط "Create"
7. اختر القاعدة من القائمة اليسرى
8. اضغط على تبويب "Import"
9. اختر ملف database.sql
10. اضغط "Go"

الخطوة 4: تعديل إعدادات الاتصال (إذا لزم الأمر)
-----------------------------------------------
افتح ملف config.php وعدّل:

define('DB_HOST', 'localhost');     // عنوان الخادم
define('DB_USER', 'root');          // اسم المستخدم
define('DB_PASS', '');              // كلمة المرور (فارغة افتراضياً)
define('DB_NAME', 'alwakeel_db');   // اسم القاعدة

الخطوة 5: تشغيل النظام
------------------------
1. تأكد من تشغيل Apache و MySQL
2. افتح المتصفح واذهب إلى:
   http://localhost/alwakeel_interface/alwakeel.php

=====================================
🎯 استخدام النظام:
=====================================

1. تبويب الأوامر:
   - عرض جميع الأوامر
   - إضافة أمر جديد
   - تحديث القائمة

2. تبويب النتائج:
   - عرض جميع النتائج
   - إضافة نتيجة لأمر معين
   - تحديث القائمة

3. تبويب شل SQL:
   - تنفيذ استعلامات SQL مباشرة
   - عرض النتائج بصيغة JSON

=====================================
🔌 استخدام API:
=====================================

رابط API: http://localhost/alwakeel_interface/api.php

العمليات المتاحة:
------------------

1. الحصول على جميع الأوامر:
   GET: api.php?action=get_commands

2. إضافة أمر جديد:
   POST: api.php?action=add_command
   Body: {"command_text": "نص الأمر"}

3. تحديث حالة أمر:
   POST: api.php?action=update_command_status
   Body: {"command_id": 1, "status": "completed"}

4. الحصول على جميع النتائج:
   GET: api.php?action=get_results

5. إضافة نتيجة:
   POST: api.php?action=add_result
   Body: {"command_id": 1, "result_text": "نص النتيجة"}

6. تنفيذ استعلام SQL:
   POST: api.php?action=execute_sql
   Body: {"query": "SELECT * FROM commands"}

7. الحصول على الإحصائيات:
   GET: api.php?action=get_stats

=====================================
📝 أمثلة استخدام API:
=====================================

مثال 1: باستخدام cURL
-----------------------
curl -X POST http://localhost/alwakeel_interface/api.php?action=add_command ^
  -H "Content-Type: application/json" ^
  -d "{\"command_text\": \"أمر تجريبي\"}"

مثال 2: باستخدام PowerShell
-----------------------------
$body = @{
    command_text = "أمر من PowerShell"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost/alwakeel_interface/api.php?action=add_command" `
  -Method POST `
  -Body $body `
  -ContentType "application/json"

مثال 3: باستخدام Python
-------------------------
import requests
import json

url = "http://localhost/alwakeel_interface/api.php?action=add_command"
data = {"command_text": "أمر من Python"}

response = requests.post(url, json=data)
print(response.json())

=====================================
🛠️ حل المشاكل:
=====================================

مشكلة: لا يعمل الموقع
الحل: تأكد من تشغيل Apache و MySQL من XAMPP

مشكلة: خطأ في الاتصال بقاعدة البيانات
الحل: تحقق من إعدادات config.php

مشكلة: خطأ 404
الحل: تأكد من المسار الصحيح للملفات

مشكلة: لا تظهر البيانات
الحل: تأكد من استيراد ملف database.sql

=====================================
⚠️ ملاحظات أمنية:
=====================================

⚠️ هذا النظام للاستخدام المحلي فقط!
⚠️ لا تستخدمه على الإنترنت بدون تأمين
⚠️ شل SQL مفتوح - يجب تأمينه في الإنتاج

للاستخدام في الإنتاج:
- أضف نظام مصادقة
- قيّد صلاحيات شل SQL
- استخدم HTTPS
- فعّل حماية CSRF

=====================================
📞 الدعم:
=====================================

إذا واجهت أي مشاكل:
1. تحقق من ملف error.log في XAMPP
2. تأكد من تشغيل Apache و MySQL
3. تحقق من إعدادات config.php

=====================================
✨ المميزات:
=====================================

✅ واجهة عربية جميلة
✅ API كامل ومتكامل
✅ شل SQL مدمج
✅ سهل التثبيت والاستخدام
✅ لا يحتاج npm أو Node.js
✅ يعمل على XAMPP مباشرة
✅ كود نظيف ومنظم
✅ دعم كامل لـ UTF-8

=====================================
تم بناؤه بواسطة مانوس (Manus) 🤖
=====================================
